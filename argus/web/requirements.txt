# Runtime dependencies for the Argus dashboard image (argus/web).
# The generator core (pydantic/PyYAML) is installed from the repo alongside these.
Flask>=3,<4
gunicorn>=21
peewee>=3.17
psutil>=5.9
docker>=7
